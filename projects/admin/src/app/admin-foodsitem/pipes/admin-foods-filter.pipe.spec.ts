import { AdminFoodsFilterPipe } from './admin-foods-filter.pipe';

describe('AdminFoodsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminFoodsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
